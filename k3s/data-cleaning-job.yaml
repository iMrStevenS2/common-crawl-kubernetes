apiVersion: batch/v1
kind: Job
metadata:
  name: data-cleaning
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: cleaning
          image: data-cleaning
          imagePullPolicy: IfNotPresent
          volumeMounts:
            - name: data
              mountPath: /data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: data-pvc
